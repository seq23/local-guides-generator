# _screenshots (Mac-only)

This folder holds inline screenshots referenced by the Training SOPs.

Rules:
- Use the exact filenames listed in SCREENSHOT_CAPTURE_LIST.md
- Keep images lightweight (PNG recommended).
- Do not rename files once referenced (links will break).
