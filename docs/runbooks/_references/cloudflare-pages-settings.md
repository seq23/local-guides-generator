# Cloudflare Pages settings (record)

**Root directory:** tacg_city_template_project_v5 (or your chosen project subfolder)  
**Build command:** `npm ci && npm run build`  
**Output directory:** `dist`

## Notes
- `npm ci` requires a `package-lock.json` in the root directory you set above.
- If you change the root directory, the lockfile and `package.json` must live in that root.
